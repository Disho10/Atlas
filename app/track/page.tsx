import TrackClient from './TrackClient';

export const metadata = {
  title: 'Track your order · Atlas',
  description: 'Track your Atlas order or start a return or exchange using your order number.',
};

export default function TrackPage() {
  return <TrackClient />;
}
